volumes:
  pg_data:

services:
  db:
    image: postgres:16-alpine
    container_name: dev_db
    restart: unless-stopped
    env_file: .env
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data